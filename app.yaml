#Basic python3 runtime
runtime: python37
runtime_config:
  python_version: 3.7

#Application
env: standard
default_expiration: "7d"

#Handlers
handlers:
  - url: /static
    static_dir: static
  - url: /.*
    script: auto
