#Basic python3 runtime
runtime: python37
    apt_version: 1
    threadsafe: true
runtime_config:
  python_version: 3.8

#Application
env: standard
default_expiration: "7d"

#Handlers
handlers:
  - url: /static
    static_dir: static
  - url: /.*
    script: auto 
